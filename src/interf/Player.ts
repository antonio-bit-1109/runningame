export interface IPLayerConfig {
    x: number,
    y: number,
    width: number,
    height: number,
    velocityY: number,
    isJumping: boolean,
}

export interface IcoordinatesElem {
    x: number,
    y: number,
    width: number,
    height: number,
}